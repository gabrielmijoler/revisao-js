export function somavet(vet){
    let soma = 0 
    for (let n of vet)soma += n
    return soma
}

export function quadradovet(vet)
// cria um novo veotr cujos elementos equivalem
// aos elementos do veotr original elevados ao quadrado
let res = []